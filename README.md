# E-commerce Product Classification

This project uses a CNN model to classify product images into categories.

## Model
- CNN built with TensorFlow/Keras
- Input size: 224x224 images
- Output: Product category prediction

## How to run

Install dependencies:
pip install -r requirements.txt